# ADR NNNN — <Short imperative title>

- **Status**: Proposed | Accepted | Superseded by ADR-NNNN
- **Date**: YYYY-MM-DD
- **Decider(s)**: <name(s)>

## Context

<!-- What's the situation that forces a decision? Concrete: include the
     symptom, the user-visible behavior, the failing constraint. Avoid
     "we should..." — that's the decision section. Include enough detail
     that someone reading in 6 months understands WHY this came up. -->

## Decision

<!-- The actual choice, in one paragraph. Be specific:
     "We will adopt zod for API contracts" — not "We will consider
     adding runtime validation." If the decision has subparts, use
     a short numbered list. -->

## Consequences

<!-- What ELSE changes because of this decision? Both directions:
     - What gets better (less coupling, less drift, etc.)
     - What gets worse (new dep, more boilerplate, learning curve)
     If a known follow-up is required, name it. -->

## Alternatives considered

<!-- Optional but recommended for non-trivial decisions. Each
     alternative + one-line reason it was rejected. This is the
     section that pays off when someone re-asks the same question
     a year later. -->

<!--
    ─── Authoring notes ────────────────────────────────────────────
    1. Numbering is sequential. Check `docs/adr/README.md` for the
       next number. No gaps.
    2. Filenames are kebab-case: `NNNN-short-slug.md`. Match the
       title's substantive verb.
    3. Once committed, the file is IMMUTABLE except for the Status
       line. To revisit a decision, write a new ADR that references
       and supersedes the old one. Keep the old file in place — the
       historical reasoning matters.
    4. Add the new ADR to the index table in `docs/adr/README.md`.
    5. Keep it short. Most existing ADRs are 50–150 lines. If yours
       is longer, ask whether two ADRs would be clearer than one.
-->
