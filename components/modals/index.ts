// Dialog overlays. Every modal here uses `useFocusTrap` and the
// `role="dialog" aria-modal aria-labelledby` triad — see the README a11y
// section. Keep new modals in this folder for consistency.
export { default as CaseModal } from "./CaseModal";
export { default as AuthModal } from "./AuthModal";
export { default as ConfirmDialog } from "./ConfirmDialog";
