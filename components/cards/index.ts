// Case-card variants used in the grid and the featured row.
//
// `BentoGrid` and `QuoteCard` were removed in May-2026 when the Atlas
// landing collapsed into the same uniform `case-grid` as every other
// section. The hero / pull-quote interleave they implemented no
// longer had a consumer.
export { default as CaseCard } from "./CaseCard";
export { default as FeaturedRow } from "./FeaturedRow";
